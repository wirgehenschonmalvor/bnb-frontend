import { ComponentDefinition, ResourceLoaderOptions } from '../types';
declare const normalizePath: (path: string, resource: ComponentDefinition, options?: ResourceLoaderOptions) => string;
export default normalizePath;
