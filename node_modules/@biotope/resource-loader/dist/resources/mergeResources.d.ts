import { Resource } from '../types';
declare const mergeResources: (resources?: Resource[]) => {
    dependencyPaths: any[];
    elements: any[];
    path: string;
}[];
export default mergeResources;
